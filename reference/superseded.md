# Claims that are dead, and must not be restated

`tools/facts.py` reads the table below and fails if any of these phrasings appears in a document
outside a correction. `make facts` runs it and so does the pre-commit hook.

**Why this file exists.** An audit on 8 August 2026 found eleven places where the documents
contradicted the code. Five of them were claims a later finding had falsified: `findings.md` had
been corrected in place, as the house convention asks, but nobody swept the documents that
summarise it. Correcting the original is not enough when four other files repeat it, and
remembering to sweep is not a mechanism.

**How to use it.** When a finding overturns something, add the dead phrasing here in the same
commit that lands the finding. That is the fourth place a confirmed fact goes, after
`config-format.md`, `findings.md` and a regression test; the `finding` skill says so too.

**A phrase may still appear inside a correction**, because recording corrections in place is the
whole point of the convention. The checker allows a line that starts with a blockquote `>`, a
strikethrough `~~`, an italic note or a bullet, or that sits directly above or below one. So
quoting a dead claim in order to say it is dead is fine, and asserting it is not.

**A line opening in bold is not a correction**, and it counted as one until 8 August 2026, because
the italic marker `*` also matches `**`. These documents open a load-bearing sentence in bold
constantly, 507 lines of them, so the exemption was widest exactly where a summary would restate a
dead claim: 19% of all non blank lines were excused, against 10% now. Two real restatements were
being missed and both are marked with the explicit escape below.

**Choose the phrase carefully.** It has to be dead in every context, not merely dead in the one
that prompted it. `40588 glyph codes` is *not* here, because it remains the correct figure for the
three architectures whose glyphs are two bytes a pixel; what was wrong was quoting it as the corpus
total. Where a number is only dead in one reading, mark the live one with a `fact:` marker instead
and leave this table alone.

| phrase | superseded by | what is true instead |
|---|---|---|
| `pools devices at account level` | section 218, corrected 30 August 2026 | a household holds account records and each holds exactly one remote, so an account is one remote's world and its devices are that remote's; measured over 21 account records |
| `another reader will not move it` | section 53 | seven readers moved it, from 26.3% to 98.1% on a Harmony 700 |
| `still carries no `data-scan` attribute` | the traced drawings, 21 August 2026 | a drawing states a code on the keys `reference/button-maps.md` names and no attribute on the rest |
| `Drawn rather than traced` | the traced drawings, 21 August 2026 | the geometry is traced from Logitech's own documentation, by Danny's decision, and the lab holds the trace |
| `placement is schematic` | the traced drawings, 21 August 2026 | a traced key is where it is on the product, so a hit region may be taken from the coordinates |
| `Twelve are named now` | section 47 | all twenty base slots are accounted for |
| `20260 programs` | section 64 | 20374, once arch 9's mode programs became reachable |
| `18252 programs across ten configs` | section 53 | 20374 across thirteen containers |
| `have no such region` | section 62 | arch 9 has a picture region; only the safe mode containers do not |
| `no header and no framing` | section 54 | a picture states `u8 kind; u16 stride; u16 rows` |
| `nothing found so far addresses them` | section 64 | screen opcode 2, inside mode programs that were unreachable |
| `width on arch 14 is not established` | section 54 | `stride` states it, like every other architecture |
| `decode only 43 of 114` | section 64 | 114 of 114 |
| `which of 0xFE and 0xFF is which is not established` | section 59 | two pages, and `0xFE` maps from program address zero |
| `route to the MCU_ID that would measure` | section 59 | unreachable by construction: the window is two 64 KiB pages |
| `the pictures do not tile` | section 54 | they tile exactly, once `stride` is read as pixels |
| `They are not copies of the page lists` | section 69 | they are copies, differing only in which base slot 10 entry they name |
| `What the twin is for is not established` | section 69 | it is a copy of the page's own list, and nothing reads it |
| `never at the same rank in their pools` | section 69 | the k-th copy is the k-th page, in mode table order |
| `address a second operand space` | section 72 | the operand carries the rest of the opcode, and `0xC000` is the lowest band the dispatcher tests |
| `nothing found so far sends data during one` | section 91 | a literal scan cannot see a data response at all, since its length nibble is computed; CCP2 is a capture on both edges in all four images |
| `the protocol knowledge was moved to the server between the two generations, and the server is gone` | sections 56 and 58 | the MyHarmony service answers and compiled a config that week; the classic service is the discontinued one |
| `a second dispatcher, not read yet` | section 73 | both dispatchers are read to the end, band by band |
| `consume the next three off the queue` | section 73 | three bytes, not three instructions: the queue reader pops one byte |
| `unknown; in the second operand space` | section 73 | `0x1F` is a register machine and `0x07` thirteen nullary operations |
| `About a third of the entries are drawn` | section 66 | all of an arch 12 bank, all but two elsewhere |
| `an entry is four bytes` | section 66 | `6 + 3 * pages`; the four byte reading was short rather than wrong |
| `arch 9 uses a different packing` | section 63 | read: two bits a pixel, rows framed by their own byte length |
| `PROFILES` entry is already in | 8 August 2026 | it was not, until the audit added it |
| `one instruction, not two` | section 74 | two instructions: arch 12 tests both, and arch 14 issues neither |
| `75 to 80% on the One` | section 74 | 97.0%, once the beeper, the date step and the silent write were read |
| `The header is 21 bytes` | section 75 | `12 + 9 * count`, stated at `+0x0B`; 21 is the count of one case |
| `wanting a firmware nobody has` | 8 August 2026 | `concordance -b -f` returns the whole firmware region on arch 8 and arch 9, so an image is one contributor away; and most of arch 8's remainder is self framed and needs none |
| `does not return firmware.` | section 2, 8 August 2026 | it does on arch 8 and arch 9; the defect is two architecture entries, so the claim is only true with a scope, and a sentence that ends there has none |
| `flag rather than an address bit` | section 76 | bit 23 belongs to the read command's address on arch 9; a 525 is silent below `0x800000` and answers `AHCM` at `0x820000` |
| `which the lab does not have` | section 76 | it has one: the arch 9 application image was read off a 525's external flash at `0x810000` on 8 August 2026 |
| `twelve byte version block` | section 76 | seven on a 525, and the reply's low nibble states it; twelve is the arch 12 and arch 14 figure |
| `every owner is rebuilt except base slot 0` | section 77 | every owner the accounting claims is rebuilt; what stays copied is what no reader claims |
| `the one thing the emitter cannot touch` | section 77 | base slot 0 is read and rebuilt from fields |
| `payload, starting with FRAME_PROLOGUE` | section 77 | the payload is a list of nodes, and those nine bytes are the first one |
| `no field inside it has ever been read` | section 77 | tag, length, level, index and name, and level 1 names base slot 13 |
| `the glyph count on arch 12, and 1 on arch 8` | section 78 | the byte at +1 is the first glyph code; the count sits at +2 unless that byte is zero |
| `Which of the two header bytes holds it is` | section 78 | it is the byte at +2, and the other one is the first code, not a spare |
| `the font header's spare byte` | section 78 | there is no spare byte: it is the first glyph code |
| `version word is per model` | section 81 | per config: one Harmony One carries two words either side of one sync |
| `word is per model rather than per config` | section 81 | the same unit disagrees with itself, section 58's observed pair |

| `66 shared descriptors` | section 82 | 5 symbol tables; the 66 came from reading each block area's start, a body start only 135 times in 199 |
| `Nothing here decodes that` | section 82 | the body is `u24 table; u16 n; u8 index[n]`, and every width is a literal in the firmware |
| `what stays copied is 4 to 68 bytes` | section 84 | nothing stays copied: every byte of eighteen containers is written by a rebuilder |
| `43 bytes left in six gaps` | section 84 | the six shapes are read, and no user config has an unattributed byte |
| `it takes eleven` | section 85 | one, a row index; the picture belongs to the opcode 3 that follows |
| `all 912 instances` | section 85 | 1080 and 776 row selects, eight per mode page, and the address is opcode 3's |
| `eight byte values are not decoded` | section 86 | a transition: `u8 zero; i16 from; i16 to; u16 operand; u8 opcode` |
| `the u16 at +0x02, unexplained` | section 86 | the variable's highest value, which its name states plus one |
| `next free number inside its own family` | section 81, corrected 9 August 2026 | the first free number above the contiguous run containing that remote's own skin: Gin's block is 54 alone and 55 is allocated elsewhere |
| `+0x04  u8    zero in every sample` | section 77, widened 9 August 2026 | the frame's length is a `u24` at +0x02, so that byte is its high byte; no corpus sample separates the readings |
| `0 = normal, 2 = Test mode` | section 87 | 0 is application mode, 4 safe mode, 1 Test mode, 3 Boot mode, from the packages' own comments |
| `Low nibble a compiled in zero` | section 87 | field 4's low nibble is the software type; it reads 4 in each remote's safe mode image |
| `PROTOCOL, SKIN, FLASH and BOARD. That is what a remote compares` | section 87 | six fields, and an absent or empty one matches anything |
| `a u24 duration in units of 0.1 microseconds` | section 92 | nanoseconds, and the two values are a carrier period and its fifty percent on time |
| `count % FLASH_CHUNK_DATA == 1` | section 94 | the refusal is an odd count: the fetch loop reads a word, subtracts two and exits on zero, so 65 and 127 hang too |
| `caps an internal read at one chunk` | section 93 | it refuses a count whose final chunk would be one byte; 64 and 124 byte reads are measured safe |
| `the payload is the last N bytes of the file` | section 87 | the header ends at the line carrying the INFORMATION terminator; the declared length is a check on that split |
| `at or above program address` | section 96 | there is no address threshold: the response sender has no bound, and the read returns if the flash byte `0x8C7` above the failing chunk is even |
| `The parity rule is real but it is local` | section 96 | an odd count never terminates anywhere; what varies is whether the overwritten counter lands on an even value |
| `a comparison somebody can find` | section 96 | there is no comparison; the deciding byte is 2247 bytes further into the data the loop is reading |
| `is no longer the obvious next thing to read` | section 98 | nothing in the response machinery was: the endpoint's buffer descriptor is pointed straight at the capture buffer |
| `the sort of coincidence that says one codebase` | section 98 | `0x40D` is the endpoint's byte count, fixed by the part, so all three would share it however they were written |
| `The header layout is arch 12 only` | section 98, corrected the same day | arch 14 writes the same header through `INDF0` at `0x0938C`; the scan that missed it filtered out `0xEF` |
| `a polite end is a reboot, or it is nothing` | section 99 | USB mode has an exit gated on the command state being zero, which is exactly what `0xE0 0x01` clears |
| `it is the disconnect, not the traffic` | section 99, 10 August 2026 | a session of one plain read then a cable pull left the remote out of USB mode; both sticking sessions had contained a deliberate odd count hang |
| `a self-clearing restart rather than a battery pull` | section 100 | a genuine device reset: the clock is reset too, so data memory is reinitialised and no corruption survives the hang |
| `No arch 9 firmware routine has been traced to it` | section 101 | `0x046D6` reads it and `0x038EC` derives from it; the transfer sends `0xB0 | row`, a page address command |
| `the return matching opcode 22; one per mode program` | section 101 | that is arch 12's reading; on arch 9 opcode 23 is the page transfer, paired one to one with opcode 22 |
| `a peripheral operation selected by operand bits 4 to 8` | section 102 | arch 14's description on arch 12's handler; the field is five bits and selects between three mechanisms |
| `their contents are **unread**` | section 103 | group 9's fourth device level pair and a table of two bit fields, reached by overrunning the group on purpose |
| `so that is a shape rather than a reading` | section 103 | not six `u16` values with a missing count byte: four bytes are a device level pair and eight are read as bytes |
| `not gaps: `0x1F` band `0xFC`` | section 104 | `0x1F` band `0xFC` is intercepted by the instruction fetch before the dispatcher sees it, on all four architectures, so the dispatcher's arm doing nothing is not the instruction doing nothing |
| `Read as far as it goes in section 102 and it stays placement` | section 103 | selector 17 sets the display's light level, which is a meaning, and it is 68 of the band's 106 uses per config |
| `an unnamed peripheral` | section 103 | `CVREF`, the comparator voltage reference, whose 27 distinct settings the level table is |
| `What they threshold is not established` | section 103 | the four sample sum of analogue channel 1, giving a band that chooses the display light's level and the device levels it sends |
| `a conjecture rather than a finding` | section 105 | it is a finding: the scale is 4 + trim/65536 millivolts a count from two words in page 0xFF, and the firmware compares the result against the literal 3400 |
| `The four small records in the `0xFF` page.` | section 105 | `+0xF580` is the battery gauge's scale, and its second word is the per unit trim two Harmony Ones differ in |
| `four timeout pairs` | section 106 | four pairs of device levels: `0x249A0` sends both halves to the I2C device's registers 2 to 5 and nothing counts them down |
| `program the band's timeout` | section 106 | it sends the band's pair of eight bit levels to a device at I2C address 0x60 |
| `the operand's bit 0 chooses which of the two` | section 106 | selectors 0 to 12 read bits 1 to 3 normalised to a boolean at `0x24F6C`, not bit 0; bit 0 is the display light's fade against snap and belongs to selector 17 alone |
| `That is the shape of a bit banged output` | section 106 | it is an enable: set at the end of a power up sequence and cleared at the start of a power down, with the data going over the hardware I2C master; the loop that gave it that shape has no callers |
| `the only structure in the format so far that is not one table across architectures` | section 107 | one of three: the opcode block `0x65` to `0x6E` is arch 14 only, and arch 9 and arch 12 branch every one of those ten opcodes to the dispatcher's exit, and section 139 adds `0x0F`'s bands |
| `Two structures are not one table` | section 139 | three, since `0x0F`'s bands were read from arch 12 and arch 14 and answered for arch 9 as well. Count them where a document states the number, or say "not one table" without one |
| `0x3F`'s bands are the only structure in the format that is not | section 107 | the same, from the other document; two structures diverge and the second is ten whole opcodes |
| `two more accumulator operations, through helper routines` | section 107 | a 16 by 16 multiply and a restoring division: `0x78` takes the product's low word and `0x77` the quotient |
| `exactly one opcode in the whole corpus has no reading` | section 107 | none: `0x6E` was the last and it is the accumulator modulo the operand |
| `emit one to three bytes on a diagnostic channel` | section 108 | it appends them to a region of the external serial flash, through a page program with a status poll |
| `peripherals and a diagnostic output channel, plus register moves` | section 108 | the channel is the flash journal: `0x0F`'s `0xE0` band appends bytes to a region of the external flash |
| `2 MiB by three routes and 4 MiB by concordance's` | section 108 | not open at all: section 88 closed it at 2 MiB on 9 August 2026 and the summary was never swept |
| `the corpus already records per remote as a manufacturer and device byte pair` | section 109 | the JEDEC pair is a capacity code and a manufacturer: the firmware compares one against 0x13, 0x14 and 0x15 to choose a flash size, and only a capacity code can do that |
| `Poll a candidate pointer variable while operating the remote by hand` | section 110 | a remote on USB is in USB mode, so its normal interface cannot be driven by hand; on arch 12 a variable can still hold a real value and on arch 14 nothing loaded the config at all |
| `a remote on USB never runs its application` | section 111 | it never runs its **keypad handler**; USB mode is a mode of the application's own loop, and a connected Harmony One's clock ticks, `TMR1` runs and its display light state machine holds config derived values |
| `does not run its application` | section 111 | the same claim in section 48's own words: the keypad scanner is what does not run |
| `A remote on USB does not load its config at all` | section 111 | true of arch 14 only, measured on the 600; a Harmony One on USB has read its base slot 15, because arch 12 executes its config in place and has no load step to skip |
| `a remote on USB has not loaded its config` | section 111 | the same, from the summaries: it is per architecture, and the architecture it was measured on is the one where it holds |
| `its two bytes are the wrong way round` | section 111 | nothing is the wrong way round: arch 12 reports manufacturer then device and arch 14 reports JEDEC capacity then manufacturer, and `reference/concordance-notes.md` had both right before section 109 |
| `took from concordance and never questioned` | section 111 | it had been questioned and got right in two places; what section 109 found was two summaries flattening the two orderings into one |
| `Reading 0x3FFFF0 off a Harmony One would say whether those` | section 111 | it was read, twice: the block on 9 August 2026 and the sixteen bytes on 10 August, and they are not erased |
| `Whether the clock is also initialised from the record is unconfirmed` | section 111 | it is confirmed: a power cycled Harmony One read its config's build date exactly and its build time plus its uptime |
| `carry into the minute` | section 111 | `0x20072` is charger and USB housekeeping that runs once a minute; where the minute is incremented is not located, and no direct write to `0x109` exists in the image |
| `the remote sets its clock from it at every boot` | section 111 | an **arch 12** remote does; arch 14 and arch 9 carry the same record and neither has been power cycled and read, and an unscoped claim here was three sentences in `CLAUDE.md` within an hour of the finding |
| `no inline string codes at all` | section 112 | 1435 codes in 179 strings, all resolving to a non-NULL glyph; the claim was true of the decoder, which refused arch 9 glyphs when it was written, and section 85 read the packing without anyone revisiting the sentence |
| `Activity **names** are not found` | section 112 | they are drawn by a mode page's screen program and read back now, alphabet per typeface; what is not found is which activity a given name belongs to |
| `none of it decodes as ASCII` | section 112 | the arch 9 safe mode container's codes are ASCII outright; for the rest the codes are assigned per config in first appearance order over the generator's string list, which is why they are not an encoding at all |
| `The skin id is the low byte of bcdDevice, in BCD` | section 113 | true of arch 12 and arch 14 only, where the USB convention was honoured; arch 8 and arch 9 carry the skin as a plain byte, so one formula reads an 885 as a Harmony 655 without failing |
| `there is no skin 102` | section 113 | there is, a Harmony Ultimate One; the invented negative control that justified BCD in a test, whose conclusion held for arch 14 on other grounds |
| `Returns the first block whose chain validates` | section 113 | the 525's image holds a Microchip stock descriptor that validates as well as the real one, so the chain walk is a necessary test being used as a sufficient one; the block naming Logitech wins now |
| `not a container: the lab table holds firmware images too` | section 114 | an arch 8 firmware image is a container, or holds one at flash `0x01E000`, so "whatever parses" silently admitted a program image to a corpus wide percentage |
| `duplicated rather than derived, so adding a sample on the Python side and not here` | section 113 | duplication is what allowed the drift, not what caught it: `h525_config_2` had a golden vector nothing read, and the two lists are compared now |
| `What is open is whether that id is shared` | section 195 | it is shared: a Harmony 300 and a Harmony 350 both enumerate on `0x046D:0xC124` and carry different `bcdDevice` words, the same arrangement as a Harmony 600 and a Harmony 700 on `0xC122` |
| `The skin id is the low byte of bcdDevice` | section 195 | true only of a skin below 100, which is nine of the ten words known; the whole word is BCD of `1000 + skin` and a Harmony 350's `0x1104` is what shows it, so the high byte section 113 called a constant is the carry |
| `a skin does that and a skin needs the device opened` | section 195 | `bcdDevice` is in the descriptor an enumeration returns, so a skin comes from a listing with nothing opened; what a listing cannot give is a skin of 100 or more, which the low byte does not hold |
| `A generation number that stopped advancing at 12` | section 116 | field 6 names a platform, and arch 12 and arch 14 are one platform under it; four images could not tell that apart from "equals the architecture, except once", and eleven can |
| `the arch 8 safe mode image` | section 116 | there is no arch 8 safe mode image here: what `concordance --dump-safemode` returns on arch 8 declares software type 3, which is Boot mode, so it is the bootloader and the flag names the command rather than the contents |
| `base = end_addr - (offset_of_end_marker` | section 117 | the marker subtraction is right on 23 of 24 containers and 864 bytes wrong on `H890-Bedroom-2`, and it was circular: the check meant to validate it re-derived its own premise, so no input could fail it. The base is anchored on the clock record now |
| `Exact on all sixteen samples` | section 117 | said of the base recovery, and it is not exact: the corpus has 24 containers and one of them defeats it. The anchored recovery is the one that holds on 26 of the 27 there are now, and it refuses the 27th rather than guessing |
| `The framing verifies, the end address lands on the end marker` | section 117 | on arch 10 that was a tautology, since the base came out of the marker; with the base anchored on content, `H890-Bedroom-2` fails that check |
| `23 is a length with no derivation` | section 117 | it has one: all 1330 placements of three insertions were scored and the best reaches 34 of 47 against 47 for arch 8, 9 and 14, with five readers satisfied by none of them. So arch 10 is not a relabelling, which is a stronger statement than an absent derivation |
| `the page address command of the SSD1306 family` | section 101, corrected 10 August 2026 | `0xB0 \| page` is common to the SSD1306 and the ST7565 families, so it names a family of families and cannot name a part. The 525's driver carries six ST7565 only commands and none of the ten SSD1306 only ones, and an SSD1306 cannot be brought up without its charge pump command |
| `it settles it by naming the part rather than by describing the arithmetic` | section 101 | the arithmetic was right and the part was wrong, which is the reverse of what that sentence claimed was the strong half |
| `the remaining 912 sit in programs base slot 11 addresses directly` | section 118 | base slot 11 holds 22 programs containing two opcodes, 0 and 17, so it carries no opcode 22 at all; the 912 is the mode records reaching the same page programs a second time, and 1992 was a walk over 293 roots of which 157 are distinct |
| `114 mode records times eight` | section 118 | that product is a coincidence of the two user configs: the arch 9 safe mode container has 44 records and 348, because one page program draws four rows rather than eight. The mechanism is that every mode record root is also a page program address |
| `Arch 9 serves no misc read at all` | section 90, corrected 12 August 2026 | selector 1 does serve, and its answer came back as zero because the reply is `C2 <selector> <high> <low>` and this project's decoder took the byte after the selector as the value. Every other selector is genuinely unimplemented: the executor clears two result bytes and only selector 1 enters a body |
| `and a real generator got that wrong` | section 122 | said of `end_addr` in a writer rail, on the strength of one Harmony 890 config. That file is a read with 16 duplicated 54 byte chunks in it, and no generator in the corpus emits an inconsistent `end_addr` |
| `The 864 bytes are all in the final section` | section 122 | they are not in the config at all: the two 890s hold containers of the same 396225 bytes and every section in the pair is the same length, the last one included |
| `the file is not damaged in transport` | section 122 | it is, and the argument for it was circular: `<BINARYDATASIZE>` is written by the same tool after the same transfer, so it counts the duplicated bytes too |
| `the same value in a remote's safe mode image as in its application` | section 118, then section 190 | a live Harmony 525 in safe mode reports field 6 as `0x00` where the same unit reports `0x09` running normally, and the arch 8 bootloaders report zero too. **The general form stays dead and the arch 12 half is now measured true**, section 190: a live Harmony One in safe mode reports `0x0C`, so section 116's arch 12 figure was right and section 118's "a recovery image reports zero" was the overgeneralisation. Arch 14 is still unmeasured on hardware, which is why this unqualified phrasing, which claimed both, may still not be asserted. Say which architecture |
| `a recovery image reports zero` | section 190 | true on arch 9 (Harmony 525) and arch 8, false on arch 12 (Harmony One), where a live remote in safe mode answers field 6 with its platform `0x0C`. Section 118 read one architecture and wrote the rule for all of them, which is what it had just criticised section 116 for doing in the other direction. The mechanism is section 189's: arch 9 copies its safe mode image over the application so a different generation answers, and arch 12 copies nothing |
| `something non-volatile is holding it` | section 190, scoping section 118 | true of arch 9 (Harmony 525) and not of arch 12 (Harmony One), which comes up normally after a battery pull. Ruled out structurally rather than by a second measurement: section 118 names internal EEPROM as the candidate, and neither the PIC18F87J50 nor the PIC18F67J50 has data EEPROM, per gputils' headers in section 189 |
| `there is no room for a resident safe mode image` | section 118 | true of internal program flash and false as written: arch 9 keeps **both** images resident in external flash, 64 KiB apart, and the internal region is a copy of whichever the bootloader last put there. Said unscoped it reads as "safe mode is transferred from a host", which is the opposite of what happens |
| `callbacks are optional because every call site is guarded` | section 118 | `_report_stages` in `libconcord.cpp` is not guarded, and `get_identity` calls it first, so a NULL callback takes SIGSEGV. The guard is real in `remote.cpp`, which is the file the claim was read out of: a rule confirmed on the population that was looked at |
| `the validator at `0x02E30` refuses below `0x80` and at or above `0x88`` | section 119 | that is the **default arm**. Four windows are tested above it at `0x02E14`, so the range test only sees a top byte matching none of them, and reading a validator from the middle produced a rule that refused three regions the device serves |
| `flash `0x200000`` | section 119 | it is not flash: top byte `0x20` selects the on chip EEPROM, bounded to 256 bytes, which is why concordance puts the serial at `0x200010` and why the update state cell is EEPROM byte 0 |
| `value 0 is "no activity running"` | section 120 | value 0 is an activity in every user config here. The idle value is base slot 13's `first`, which `one_config` puts at 7 where its highest is 8 and where 8 **is** bound to a key. The count section 86 drew from the wrong reason is still right, since there are `second + 1` states and one is idle |
| `the only screen instruction naming a place outside its own program` | section 121 | said of opcode 2, and opcode 4 names a glyph run in another program while being twice as common. That sentence is why nobody followed its pointer, and two thirds of the corpus's drawn text was on the other side of it |
| `the physical button map in a form code can address` | 11 August 2026 | said of the silhouettes, and the placement is schematic: every key in all three drawings sits on a horizontal axis where a real Harmony's rows are angled or curved. They say which keys exist and roughly where, and a hit region must not be taken from their coordinates |
| `23 of 35 activities` | section 124 | the figure and the population both moved: 41 of 50, with arch 8, 9 and 14 complete. Containment either way was too loose where an activity's chain enters the mode that lists the devices, and a Harmony 525's menu wraps a label across two rows |
| `Arch 12 names none` | section 124 | it names two, and both are single activity configs where there is nothing to tell apart. The proof is about a page with several: no fixed scan code to row map can exist on a touch panel |
| `arch 12 2 of 11` | section 125 | 11 of 11, and the corpus 50 of 50: the mode page's `lead` byte indexes the touch hit map, so a Harmony One's activity labels are stated rather than inferred from what its modes say |
| `arch 12 names almost none` | section 125 | it names all of them. What stands is the argument under that heading, that no string rule can do it on a touch panel |
| `AGPL for the product is deliberate` | 12 August 2026 | reversed while FreeHarmony was still a placeholder. The network clause fires when a program is offered to users over a network, which a desktop application is not and a network client never becomes, and the Affero variant is a one way door with concordance and harmony-decompiler: their GPLv3 code can come here and nothing of ours could go back. The product is GPLv3; if a shared server is ever built, the clause is that server's. The argument does not rest on the application staying offline, which was the first reason given and is a separate, open product question |
| `consumes `packages/codec` and `packages/usb` as a git dependency` | 12 August 2026 | written as fact and never once tried. A git install of this repository resolves no `@harmony/*` package, and Node refuses to strip types for any file inside `node_modules`, so source only `exports` cannot work for a consumer on any version. The route is abandoned: a path dependency on the sibling checkout now, published packages when the API stops moving |
| `the first free number above the run containing that remote's own skin` | section 131 | said of skins 59 and 73, and it fitted both cases while predicting nothing. They are the **Harmony One EMEA** and the **Harmony 600 EMEA**, two products in Logitech's live catalogue, which lists 80 skins where the classic client table holds 46. The arithmetic worked because each European variant was allocated immediately above its American sibling's run, so it was reading the allocation order out of the gaps it left |
| `each unallocated in Logitech's classic software table and each the next free number inside its own platform's block` | section 131 | unallocated in **that** table, which predates the MyHarmony era, and allocated in the vendor's live one. Stating the absence without the table's date reads as a property of the number |
| `every arch 8 config has exactly 37 two group records` | section 134 | it was a test's own title, false while the test passed, because its body looped over one contributor's four configs. Two arch 8 configs here have none, which is exactly the falsifier section 75 named. The count is a property of the **devices** a config drives: the thirty seven are one biphase device's records, so a config with no such device has none |
| `whatever selects the second group, it is not how much the config holds` | section 134 | the honest form of an open question, and it is answered: the second group is the same code with one biphase cell inverted, at a fixed offset, and it exists because the protocol carries a bit the sender must alternate between presses |
| `A code has no name.` | section 133 | true of the format and false of what can be read: a config generated through an account we hold can have its scan codes named, because the code a scan sends decodes to a frame and the account's catalogue and button maps name the command and the button. Thirty two buttons of a Harmony One and thirty six of a Harmony 600, `reference/button-maps.md`, with no write to anything |
| `the 700 sits exactly at its stated maximum` | section 136 | a test's own title and circular: `maxDevices` was set to 6 because both 700 configs hold six devices, and the test then asserted the configs sit at the maximum. A config with six devices bounds the maximum below and forbids no seventh. Two vendor tables say 8, and no sample in the corpus reaches any stated maximum |
| `consistent, once at the limit` | section 136 | said of the max devices column in `reference/capabilities.md`, resting on the same circle. The column is bounded below only |
| `One round of hardware settles it per architecture` | section 137 | said of whether a remote sets its clock from base slot 3, and a round was spent to find out that no measurement of a clock settles it on any architecture: base slot 13's first seven records carry the identical fields, section 130, so both mechanisms predict the same bytes. The corpus holds no separator either, all 19 containers having at least 16 raw base slot 13 records. It is a firmware question |
| `answers all zeros, as `READ_MISC` does` | section 137 | said of the arch 9 data memory window, describing the memory when it describes the window. It answers zero for the bank 2 bytes holding the offset and buffer pointer of the read that is answering, which cannot be zero, so the window returns zeros regardless of content. The measurement it rested on was of a remote stranded in safe mode with no config loaded |
| `it is also what the remote's clock is set to` | section 138 | said of base slot 3's build timestamp on arch 12, and the clock carries that value without being set from that record. State variable `n` lives at `0x108 + n`, the firmware seeds each one from its base slot 13 record's `first`, and records 0 to 6 are the clock, so the clock **is** base slot 13's array in RAM. Base slot 3 is the epoch the firmware subtracts against to compute how long ago the config was built |
| `the seven must never be reused for anything else` | section 138 | the right rail with the wrong width. The firmware owns thirteen: variables 7 to 12 state the identical value and maximum in every container of their architecture and base slot 0 names none of them, where index 13 is named by two configs and varies inside an architecture |
| `pins 0x4321 from the data side` | test sweep, 13 August 2026 | said of the trailer checksum seed by a test that was algebra rather than a measurement: it computed `trailer_checksum(blob) ^ SEED` and required that not to equal the stored value, which reduces to `SEED == 0` and so held for any nonzero seed whatever the bytes were. The data cannot pin the seed beyond the recompute test, since the checksum is XOR linear in it; where the value comes from is the boot validator's two literals, `0x21` then `0x43` into an adjacent pair, on three images |
| `Every 0x70, 0x71 and 0x72 operand's low byte is under 64` | test sweep, 13 August 2026 | measurably false, and stated by a test whose body only filtered `0x71`. Over seven configs `0x70` reaches 81 and `0x72` reaches 83. 64 is not a firmware constant: the rule is base slot 13's `narrow` count, per config, and `0x71` stays below it because it compares a byte |
| `No container in the corpus has an odd body` | section 139 entry 21 | said of the trailer checksum, to explain that not folding the odd trailing byte is the firmware's behaviour rather than a tested one. 19 of the 33 parseable containers have an odd extent, spanning five architectures, and 14 of them verify their stored checksum under exactly that loop. Two of the fourteen carry a nonzero trailing byte, so folding it in would break those two and be invisible on the other twelve |
| `Nothing in the corpus reaches that path` | section 139 entry 21 | said of the flash base fallback in `packages/codec/src/gspm.ts`. Three reads of one Harmony 890 reach it and every one lands on an unaligned base, which the `end_addr_points_at_end_marker` check cannot see, because a fallback base is computed from the marker's own position. `src/harmony/gspm.py` had carried the correction since section 122 |
| `one instruction with a five bit field` | section 139 entry 25 | said of `0x80 \| n`, the state variable write, in section 73 and in `CLAUDE.md`. The index is `opcode - 0x80` over an opcode space running to `0xFF`, so it is seven bits and 0 to 127. The corpus emits up to opcode `0xC5`, index 69, over 4138 writes, and base slot 13's tables reach index 93. The code was never wrong, since the two spellings are the same byte below 128; a future editor narrowing the field to match the comment would break `calibration_h600`, whose activity variable is index 34 |
| `bit 15 is a separate flag on the high byte, which the dispatcher masks off` | section 140 | said of `0x70` and `0x71` in section 34, and the mask is real: the nibble decode at `0x0EEAC` on the Harmony 700 does exactly that. The same handler then tests bit 7 of the same byte, eighty instructions later at `0x0EF62`, on all seven firmware images across four architectures. The bit is the language's **else** arm: with it set, a comparison that comes out true zeroes the three bytes two slots ahead in the queue, so the two instructions after a flagged comparison are its two arms |
| `it takes six values, `0, 1, 2, 4, 5, 10`` | section 140 | said of the `0x7C` count beside an infrared send in section 33, over the ten configs that section was written against. All fifteen user configs give seven values, adding 3 from `arch8_config_885`. What rules out the operand being an identifier is the bound and not the set: an identifier would have to separate the records of a group, the largest holds 111, and the firmware caps the field at 100 |
| `That identity holds for all 2137 framed records in the corpus` | section 139 entry 7, recorded in section 140 | said of `2 * bits + 4`, the infrared frame length against the bit count. Both ends came from the same neighbouring record, because the locator searched from a fixed offset instead of following the header's pointers, so it was one field compared with itself. The identity is false on the right bytes: a once block carries the code, a gap, the protocol's repeat header and a silence. What replaces it is the decoded bit count against the protocol the header timings name, 32 for NEC and 48 for Kaseikyo, over 2237 records with no exception |
| `two states of one remote rather than two remotes, so counting them would double one unit` | section 142 | the reason `lab.CONTAINERS` left the Harmony One sync pair out of the corpus, decided against on 14 August 2026. A corpus wide total measures what a reader can read and not how many remotes exist, so its unit is the container; the two dumps differ because Logitech's software wrote a config between them, which is what makes that pair the evidence for section 58; and the TypeScript side had been counting all nineteen the whole time. The corpus is nineteen containers |
| `"@harmony/codec": "file:../harmony-explorations/packages/codec"` | 14 August 2026 | measured with npm and written as though it were a property of path dependencies. FreeHarmony uses pnpm, where `file:` copies the package into `node_modules/.pnpm`, so the real path stays inside `node_modules` and Node refuses to strip its types. Under pnpm the spelling is `link:`, under npm it is `file:`, and npm's `link:` installs nothing at all, so no single spelling works under both. The mechanism in the old wording was right: it works only when the resolved real path leaves `node_modules` |
| the client's supported list is the `IsEnabled` set minus the two hubs | 21 August 2026 | measured, and the qualifier was an artefact of counting the wrong things against each other: 27 product **records** against 19 **skins**, where a skin can appear more than once in the table. Skin against skin the two sets are equal, with nothing on either side. Section 145 |
| `MaxActivities` is a field no source states | 21 August 2026 | too strong in one direction and too weak in the other. The field exists in `GetAllProducts` and one enabled product states a value, the Harmony 350 at 1. What holds is that it is null on every model this project works on, and that the disabled half's flat 255 is a sentinel. Section 145 |
| the picture bank is addressed only by screen opcode 2 | section 146 | opcode 3 names one too, in the same last three operand bytes: 4548 of 4548 of its instructions across the corpus land inside their container and decode as a picture header. Arch 12 (Harmony One) emits none, which is why the reader looked complete on the architecture this project reads code on, and arch 9 (Harmony 525) emits nothing else, which is why `bitmaps` reported zero pictures for a container holding four |
| exactly two pictures per container are unreached on arch 8 and arch 14 | section 146 | the count was right and the cause was not: those two are drawn by opcode 3. Every picture in every bank in the corpus is addressed by a program, with two exceptions, both in the arch 9 (Harmony 525) safe mode container, where the file itself addresses nothing for them |
| the bank search is ambiguous on all three arch 9 containers | section 146 | three was the reader's number. With opcode 3 read the two Harmony 525 user configs have 4 and 5 addressed pictures, so exactly one start satisfies both constraints, and the ambiguity is one container. Section 139 was right about the shape and wrong about the population |

| arch 9 draws no picture from a program at all | section 148 | that was the reader looking at screen opcode 2 only. A Harmony 525 config names 2255 pictures across the corpus, every one of them by opcode 3, and its rendered pages went from 0 pictures to all of them |
| arch 9 has nothing to say about its own display size | section 148 | opcode 3 states its destination rectangle outright, so `dx + w` by `dy + h` gives 96 by 64 on all three arch 9 containers. Nine containers state it through both opcodes and agree exactly, which is the calibration that makes the arch 9 reading trustworthy |
| where opcode 3 draws its picture is unread | section 148 | `dx, dy, sx, sy, w, h`, destination first, per section 118, and now confirmed on a second architecture by a second mechanism: every destination fits its display and every source fits its picture, 3540 of 3540, where swapping the pairs puts 292 of 708 asymmetric arch 8 instructions outside their own picture |
| the per unit settings records are what a remote has that its config does not carry | section 150 | six of the seven are erased flash on all three bench remotes, so nothing has ever written them. Only `power settings` holds a byte, 94, on the two Harmony Ones and not on the Harmony 600. A settings page therefore edits base slot 15 of a config and has no per unit state to read |
| MyHarmony was checked and holds nothing | 22 August 2026 | a heading, and it overclaimed what its own section measured. What was measured is that MyHarmony's decompiled source holds no protocol constant and no keypad matrix, which is what it was opened for and is still true. The same cache holds ten copies of the client's workflow manifests, six distinct flows and 2616 lines of state machine, which is the only surviving statement of what Logitech asked its users per device and per activity. Unread beyond an inventory, and the thing FreeHarmony's device settings page is waiting on |
| every keypad binding belongs to an activity | section 151 | true of the fifteen configs here and not a statement about the format, which was the whole failure: a Harmony has a **device mode**, reached by the Devices key, where the whole keypad drives one chosen device with no activity running. Where its map lives is open, and the corpus cannot close it. A button map belongs to a device first, and an activity's map is that plus its overrides, 1096 of 1105 pairs agreeing |
| a device page shows the keypad per activity | section 151 | a page about a device shows the **device's** map, since that is what device mode is and what Logitech's software authors. A change to it has to reach every activity that inherited it, or it is invisible in the activity somebody is sitting in |
| the Harmony 600 and the Harmony 525 have a physical Devices key | section 151, from the manuals | true of the 525 and false of the 600, whose button table lists every key on the remote with no Devices among them: its screen writes the word above the **centre key** of the three below the display, and "Activity" to come back. The traced drawing in `reference/silhouettes/h600.svg` has no such key and must not grow one |
| a Current Activity item takes you back | section 151, from the manuals | the Harmony One's wording, printed as though it were the product's. A Harmony 600 says "Activity", a Harmony 525 uses its Activities key, and an 885 leaves device mode by pressing DEVICE again |
| a change to a device's button is written into every activity that drives it | section 151 | into the ones with **room** for it. A button that drives the television in one activity and the amplifier in another is the ordinary case: the Harmony One's receiver in the lab has 35 buttons, eight activities drive it, and three are its own in all eight, so writing all of them takes buttons off another device and refusing when one is in the way blocks the other 32. Write where there is room, leave the rest, and name them before the change |
| the values are bit reversed frames | section 132, corrected 22 August 2026 | they are in **transmission order**, the same order `irframe.ts` produces. The Sony digits that suggested otherwise are reversed against the logical digit, because Sony transmits least significant bit first, and our decoder reads transmission order too. 52 of 58 stated frames for one Panasonic television equal a frame decoded out of `h600_config` byte for byte, on a different model of the family, so a name crosses by comparing numbers with nothing turned round |
| `it cannot re-encode a frame` | section 152 | a record states its own timings, so the frame comes back byte for byte from five durations off that record plus the bits, 3502 of 3502. What the bits genuinely do not carry is everything **after** the frame, which is 140 distinct shapes and is copied rather than computed |
| `infrared encoder per protocol family` | section 152 | the durations do not have to come from a protocol table at all. 52 of 58 device groups in the corpus use one set of timings for every code they carry, so a command Logitech states as a bare frame value is written using the timings of any code of the same appliance a config already holds. The job that was priced as implementing nine protocol families is five numbers read off a record |
| `an encoder per protocol family` | section 152 | the same claim in its shorter wording, and dead for the same reason |
| `a work item nobody had priced` | section 152 | it was priced at nine protocol families and it is not that job. What is genuinely unpriced is everything after the frame, the repeat and the closing silence, which no rule explains: 140 distinct shapes over 3502 records, copied from a record rather than computed |
| `4029 records read under exactly one convention` | section 152 | a partition of 4965 records where the population is 4630, and no count at all for the records that read under both conventions. It is 3502 under one, none under both and 1128 under none, asserted together as a partition so that a container leaving the list moves all three |
| `1951 of 2085` | section 152 | the classifier behind that number trimmed trailing terminators and maximum length spaces before comparing gaps, and the trimming was doing the work: with the heuristics removed it is zero, because every tail carries a mark the frame does not account for. The frame rebuilds exactly and the block does not |
| `a port would be exercised by nothing` | section 154 | true of every config that had been **found** and not a statement about the format. A configuration was made instead, three favourite channels compiled by Logitech's own service, and it carries a record. Both parsers read base slot 16 now and a golden vector compares them on it, which is the only place that reader has ever been checked against anything |
| `no sample confirms it` | section 154 | the layout is confirmed field for field on a real record, and the byte accounting agrees with it independently: the gap it reported before the reader existed was 113 bytes, which is 23 for the record plus 30 for each of three digit tables |
| `the number sender. Used by no config` | section 154 | said in the base slot table, and its scope was the corpus rather than the world. One container populates it: the count is 1, a record is a method for sending a number rather than a number, and the three channels live in three action lists. **Deliberately narrow**: the same words are still true of the three unused infrared encoding classes, and a phrase broad enough to catch both would kill a live claim |
| a config with no channels has a NULL number sender slot | section 154 | it declares the section and gives it one byte, holding a count of zero. So an empty record list and an unreadable slot are different answers, and merging them would report a Harmony 890, whose slot mapping is deliberately ungated, as a remote with no channels rather than as one nobody has read |
| `The only config anywhere that populates base` | section 156 | a uniqueness claim, and `calibration_favzero` is the second. It is also the one that shows the section does not carry every favourite channel, so the phrase was wrong in a second way: base slot 16 is not where a channel with a leading zero lives at all |
| `One made config populates it and no found one does` | section 156 | said in the base slot table. Two made configs populate it now, and the row has to say what the section holds rather than only how many samples reach it: only the channels whose number survives being written as an integer |
| the minimum digit count is how a leading zero is expressed | section 156 | the obvious reading of a field the firmware treats as a floor, and it is wrong. That field is 0 in both configs that have records. A channel written `001` bypasses base slot 16 and is spelled out one digit code at a time, which is a second mechanism rather than a second value of a field. Recorded because a firmware reading alone would have concluded the opposite and nothing in the corpus could have contradicted it |
| their generator drops an account device nothing references | section 156 | an unconfirmed note from an earlier read of the same account, kept in the lab and never in a document. Six devices on the account, one of them referenced by no activity, and six groups in base slot 5. **The note was wrong in its premise, not its arithmetic**: the config it was drawn from has five groups because the account held five devices when it was built, the sixth having been removed during the import that preceded the sync. It compared a config against an account state read later without checking the two were contemporaneous, which is the mistake to carry forward |
| `20 is nowhere near anything the format struggles with` | the sequence notes in the lab, 23 August 2026 | said of a sequence pause, on the reading that it is a whole number of seconds in a sixteen bit field. It is neither: a pause is action list opcode `0x7C` inline, in **tenths of a second in the low byte**, exact on five authored values from one to twenty seconds. A byte tops out at 25.5 seconds, so their 20 second limit is a round number just under the ceiling rather than far below it, and 30 seconds would be unrepresentable |
| `The 25 may not be arbitrary` | the sequence notes in the lab, 23 August 2026 | a hypothesis that the editor's 25 step limit came from the 40 instruction action list queue, section 34. Refuted twice over by the sequence that was built at that limit: the 25 counts **pauses**, 21 sends and 4 of them, so the limit is on items rather than commands; and 25 items expand to roughly 55 instructions against the queue's 40, so they do not fit. What the run found instead is that a sequence at their limit hangs a Harmony One when its touch panel is tapped heavily, three times out of three |
| 30 of 30 emitted codes read back | section 158, corrected the same evening | true of the **number** and read as though it meant the codes were right. Their analyser identifies a rhythm, so a Sharp 15 Bit number emitted with Sony durations comes back as a Sony code carrying that number: correct bits, somebody else's rhythm, and an appliance would recognise nothing. The test is whether the family they name is the family the catalogue named, and on that measure it is five of thirteen |
| `the frames field holds one value or two joined by an underscore` | section 159 | read as the whole of Logitech's code notation. Their string has **three** slots and two of them hold content, each a sequence of items that may be a value or one of the words `Start`, `Repeat`, `Trailer`. Reading only the frames field refused every Toshiba code in the catalogue, 1026 of 2921 distinct codes, because it found the word `Repeat` where it wanted a number, and on the families that put a frame in each slot it emitted the second frame as the whole command |
| `the parameter slot may be occupied` | section 159 | said of the JVC answers, as a curiosity to tolerate. It is a frame slot, and what occupies it there is the word `Start`. Seven codes in the census put a **value** in it and a second value in the frames field, which is the case where the old reading sent half a command with nothing anywhere reporting a problem |
| `the rhythm is measured for five families of the fourteen their database uses` | section 159 | both halves moved. A sixth family has a rhythm, `Sharp 15 Bit`, documented rather than measured and judged by their own analyser reading 17 codes back to the exact number. And their database uses **33** families, not fourteen: the fourteen was counted off a census that kept four codes per family from six appliances per make, against 106 appliances and 2921 distinct codes now |
| `name agreement is the test` | section 159 | the correction section 158 landed the evening it was written, and it overshot. Their analyser's family list is coarser than their catalogue's, measured three ways: one rhythm answers for both their Sharp families, `Pioneer 32 Bit 2` comes back as `Pioneer 32 Bit`, and a 48 bit code is named from a vendor field in its own payload. So agreement on the name is sufficient evidence and not necessary, and a code whose number comes back exactly under another family's name may still be right |
| `JVC 16 Bit is confirmed through NEC's durations` | section 160 | recorded on their analyser's word, which answered `JVCO1 16 Bit` to a code built with NEC timings. Their own compiler emits JVC as 8400/4200 with 500/500/1600 against NEC's 8990/4490 with 568/552/1662, over 108 records. So the code that was accepted is 590 microseconds long in its lead in and wrong in every bit cell, and their analyser named it correctly regardless |
| `17 catalogue codes came back from their analyser carrying the exact number` | section 160 | true, and it was read as confirming the rhythm. Their compiler emits that family with a mark of 260 and spaces of 790 and 1850 against the 320, 680 and 1680 the seed carried, so every duration was out by a fifth to a quarter and their analyser read it back perfectly anyway. Together with the JVC case this retires `readBack` as evidence for a rhythm: their analyser is tolerant, and section 159 had already measured that tolerance without following it through |
| `one rhythm therefore serves both of their Sharp families` | section 160 | said of the seeded Sharp durations on the strength of their analyser accepting them for `Sharp 15 Bit` and `Sharp 15 Bit 2` alike. The seed's numbers are refuted, so what the two families share is only that neither has a rhythm in the table. Their compiler's numbers for `Sharp 15 Bit 2` are in the table now, 162 of 162 exact; `Sharp 15 Bit` without the `2` still has none, so the two families are separate in the table as well as in their catalogue |
| `their catalogue's Toshiba/Memorex/Pioneer-2/JVC/Panasonic/Sharp-48 all emit with the durations their analyser files under` | section 160 | a summary of section 158's emit check, and two of its six members are now measured as different rhythms. `Toshiba 32 Bit` does equal the NEC entry, exactly. `JVC 16 Bit` and the catalogue's `Memorex 32 Bit` do not. So the list was a record of what their analyser tolerated rather than of what their compiler emits, which is the distinction section 160 exists to draw |
| `Twelve families have a rhythm measured off Logitech's own compiler` | section 160, the same day | counted before Sharp joined. Thirteen, and the thirteenth needed one field rather than one more appliance: `firstMark`, a longer opening burst, which is the whole of what a strict reader was refusing |
| `neither a bit reversal nor the Sharp inversion carries one to the other` | section 160, corrected the same day | said of our decoding of a Sharp record against a number their catalogue states, and it was a comparison of two numbers of one appliance that were never established to be the same command. Compared as sets instead, the **identity** maps 162 of 162 of our frames onto the 246 numbers stated, a bit reversal reaches 25 and the complements none. So there was no bit order problem to solve, and the pair comparison is the same mistake as section 32's closure whose two ends came out of the same bytes |
| `Sharp is the interesting failure and it is two separate problems` | section 160, corrected the same day | one problem, plus a wrong reading of the evidence. The durations genuinely would not split, because the opening mark is 270 against 260 for every later cell. The numbers joined all along under the identity, and calling that a second problem gave a bad comparison the standing of a measurement |
| `Thirteen families have a rhythm measured off Logitech's own compiler` | section 161 | counted before the attribution was fixed. Fourteen. The join decided which appliance a group of codes belongs to by whichever appliance's stated numbers the group's decoded numbers hit most, and that returns nothing on four groups of fifteen, so 242 records were dropped whole. The config states the owner, through the device name section 126 reads, and the two routes agree on 10 of the 10 groups where both have an answer |
| `The owning appliance is decided by overlap` | section 161 | written as the rule and it is the fallback. It is a vote, so it fails where the numbers do not match for any other reason, and it once attributed a group to an appliance sharing exactly one number with it. The config names its own groups and the account names its own appliances, so the two join on a string with no number in it |
| `148 biphase codes are readable and we cannot read them` | section 162 | true for a month and the reason was that our decoder answers a different question: a biphase code has one duration, the half cell, and the bit is which half of it carries, so there is nothing to split. Read with the alignment, polarity and width settled against Logitech's catalogue, three families reproduce every one of their 226 records byte for byte, and 48 records of four contributed configs land on the numbers their analyser had already stated. `irFrame` still refuses them on purpose, since that refusal is the corpus's only detector for the family |
| `Seventeen entries, and each carries the route it came from` | section 162 | twenty. Three biphase families joined at once, and with them the table gained a second row shape: a half cell, a lead in and a polarity, against five durations and a carrier convention |
| `reading under both conventions means a two group record, and the reverse` | section 163 | a biconditional over 148 records and section 163 spent one direction of it deliberately. Requiring a frame's non carrying half to be one length, which is what lets the gap threshold rise far enough to read `JerroldO1 16 Bit`, takes those records to reading under no convention at all. They are biphase, section 162 reads them, and the detector moved to the reader that names the cause: every two group record is biphase, and the reverse fails because 109 more are biphase with one group |
| `Twenty entries, five of them measured on both routes` | section 163 | twenty one and six. `JerroldO1 16 Bit` joined once the flat length rule let the threshold rise, and the catalogue route added 208 biphase records nobody had asked their analyser about, which took `Kreatel IP 22 Bit` and `Microsoft 30 Bit` to both routes |
| `framesOfPulses reads an unmerged train and should not` | section 164 | it merges now, and the change had been decided and measured in section 153 eleven sections earlier. Two words of one kind in a row are one interval, and reading them apart gave 45 records of three arch 8 configs an eight bit frame they do not have, all 45 the same value |
| `merging turns 616 unreadable records into ambiguous ones` | section 164 | predicted in section 153 against the decoder of the day and it never happened, because section 163 landed in between: requiring a frame's non carrying half to be one length already refuses every one of them. The partition after the merge is 3502 under one convention, none under both and 1128 under none, not the 3502, 764 and 57 predicted |
| `3547 records read under exactly one convention` | section 164 | 3502, and the 45 that left were the ones a merge takes a reading away from. The other two columns are section 163's: none under both, 1128 under none |
| `the non carrying half of a frame is byte identical throughout` | section 165 | it must not **split**, by the same ratio the carried half has to split by, which is a different claim. Logitech's generator emits a flat half of 433 on one cell and 434 on the next, so exact equality refused ten records of the compiled sample that their own analyser names, nine of them on the number our reader now recovers exactly. The widest spread admitted is 6.1% and the narrowest refused is 100%, with nothing in between |
| `twelve records of the compiled sample read as nothing at all` | section 165 | ten of them read now and land on numbers Logitech's own analyser and their own catalogue both state, and the two that are left are 16 zero bits of `JerroldO1 16 Bit`, which their analyser refuses as well |
| `2852 of 2921 distinct codes` | section 159, corrected on 24 August 2026 | 2921 of 2921, and 33 of 33 families. The 69 that were refused are one family stating its digits in **base 4**, which the family's own name says: `Quad` is the base of its digits and not a count of its frames, since its codes state three values where the four families naming `Dual` state two. The width check that refused all 69 accepts all 69 under that base, so nothing was relaxed |
| `the cause is legible and deliberately not implemented` | section 159 | said of the quaternary reading for a fortnight and it is implemented. What made it cheap in the end was that the falsifier was already there: the arithmetic that refused every one of those codes is the same arithmetic that now accepts every one of them, 0 of 69 against 69 of 69 |
| `the parser refuses those 69 codes rather than guessing` | section 159 | it reads them. The digit set was corroboration and never the discriminator, which is measured: six families have codes whose digits all fall in 0 to 3, and Galaxis is the only one where that holds for every code, so a reader keyed on the digits would have had to guess on a `Sony 12 Bit` code that happens to use no digit above 3 |
| `a family naming one width and stating two values could mean 38 bits per frame or 38 across the pair` | section 166 | across the pair, read off the wire: `Samsung 38 Bit` sends one frame of 17 plus 21 bits, the section widths summing to exactly the 38 its name states, all 35 records of the compiled sample landing on their stated pairs. The boundary space is section one's final set bit and the closing silence is section two's, which is why a five duration reading refused every record |
| `refused deliberately by the one length rail` | section 170 | the two lengths in those families' flat halves are not noise on one duration: the mark rides with its own cell's bit, two exact (mark, space) pairs perfectly correlated in every record of `Panasonic 16 Bit`, `MemorexV2 32 Bit Dual` and `Sharp 48 Bit`, and `oneMark` in the rhythm table carries the set cell's mark. The refusal now applies only to a mark that varies within one space cluster, which really is a wobble |
| `admitting them would mean writing down a duration their records do not have` | section 170 | the records state both durations exactly, one per bit value, and the table entry that writes both reproduces every record to the microsecond |
| `still get no ``data-scan``` | section 133, corrected on 25 August 2026 | the traced geometry of 21 August 2026 put `data-scan` on exactly the measured keys and on nothing else: the Harmony 600's 36 mapped buttons, and the Harmony One's 32 plus the two touch arrows section 125 placed. The rule that survived is never a guess, not never an attribute; the Harmony 525 stays candidates only and its test still refuses the attribute |
| `what a binding table entry corresponds to` | sections 120 and 121, swept on 25 August 2026 | a base slot 9 entry is an activity's handler set in the four hop chain that starts it, and all fifty activities in the corpus are named through it. The claim of openness survived in two summaries for a month after the finding |
| `Harmony 600 and Harmony 700` | section 175, corrected on 25 August 2026 | as a list of bench hardware it names a remote that has never been here. The bench is two Harmony Ones, a Harmony 600 and a Harmony 525; the Harmony 700 is an arch 14 **reference image**, two configurations and a firmware image. It gets mistaken for hardware because it is the best mapped arch 14 image and is quoted constantly, and the mistake matters because a reference cannot be written to and a remote can. Naming the pair as an architecture's model family is fine and reads "the Harmony 600, or the Harmony 700 for the reference image" |
| `the flash write data path does not exist` | section 175, corrected on 25 August 2026 | it is derived and `packages/usb/src/writes.ts` builds it: an announce, `0x4A` data packets nothing answers, and a done acknowledged once from state 3. What has never happened is the **sending**, and that is the claim to make instead. The wording survived one sweep because it sits in a paragraph about backups rather than about writing |
| `two entry chain at ``0x0BD5C``` | section 175 | that address is the `SUBWF` comparing the command against `0xE0` in the escape handler, four instructions before the state gate. The state 2 chain is `0x0C55C` on the 700 image, `0x0C4C0` on the Harmony 600 and `0x26752` on the Harmony One. The description beside the wrong address was right, which is why nothing noticed: no test read the address |
| `both outcomes are informative` | section 175, corrected on 25 August 2026 before anything ran | said of writing a block back without erasing it first, to find out whether the firmware erases. The bytes being written are the bytes already there, so the AND of old and new **is** the bytes back and the two answers are indistinguishable. The rehearsal erases explicitly instead, which is correct under either answer |
| `25 erase blocks` | measured on 25 August 2026 | a Harmony One configuration spans **26** blocks of 64 KiB, and the count is per configuration rather than per model: the spare's own two here span 26 and 21. So a bare fraction is the wrong shape for it |
| `latches before it permits` | section 175, corrected on 25 August 2026 | the Harmony One's bit 5 of `0x1A4` is a **block and not a permit**, so the sentence has the polarity backwards and with it the consequence. `0x268BC` is `BTFSS`, skip if **set**, and what it skips is the branch **over** the `CLRF` that abandons the write. So a write below `0x020000` needs the bit **clear**, and the bit is set at boot and on every main loop pass, which makes the low region an interlock only a low erase opens. Same error class as the three bit test polarities `CLAUDE.md` already warns about. **This row's own explanation was wrong once too**, having said the region is open at the start of a session, which was the second of three readings |
| `nothing in the write path ever clears the bit` | section 175, corrected on 25 August 2026 | true and beside the point, because the erase path is not the write path: `0x26612 BCF 0x1A4,5` runs whenever ERASE_FLASH is given an address below `0x020000`. So an erase below the boundary is the one thing that opens the low region, which is the order an installer would use. The wording was a search that stopped at the boundary of the routine it started in, and this row first said the erase **re-armed** writing, which is wrong in the same direction: nothing had armed it, since the bit's resting state is set |
| `A host cannot lose a packet by streaming` | section 175, corrected on 25 August 2026 | the firmware half is derived and the claim is one step wider than it: the dispatcher drains the staging buffer in the same service call, which settles that the **firmware** asks for no pacing, and says nothing about whether the USB peripheral can accept a second report before the first is serviced. That is the endpoint's buffer descriptor and its ownership bit, unread here. The honest form separates the two, since the expectation about the silicon is reasonable and is not a finding |
| `the predicate at `0x0C5FA`` | section 175, corrected on 25 August 2026 | that address is a `SETF` on the destination selector, so it is not a predicate and the empty cross reference against it was measuring nothing. The returned value is computed at `0x0C600` on the 700 image and `0x0C566` on the Harmony 600, and the drain gates on the write flag alone at `0x0C614` and `0x0C578`. A wrong address with a right description beside it survived into three documents |
| `The seven command states` | section 175, corrected on 25 August 2026 | there are eight, and the missing one is **state 3**, which a finished write transfer occupies while it answers `0xF0 0x30`, executor `0x0C95C` on the 700 image. It escaped a table of command states because no host ever sends it: the done packet moves the remote there. So a survey of commands cannot enumerate states |
| `the low region is writable at the start of a session` | section 175, corrected on 25 August 2026, the second correction to one paragraph | wrong for a different reason than the first version: the bit is **set at boot and re-set on every main loop pass**, by `0x2B824` in the routine the entry point at `0x2EA38` calls first and the main loop at `0x2EA4C` calls forever. So the region is closed in the ordinary running state and opens only just after an ERASE_FLASH below `0x020000`, the one instruction anywhere that clears the bit. The error was reading a bit's meaning off the one site the current derivation walked through instead of off every site that touches it, and the test written for it passed |
| `re-arms low writing` | section 175, corrected on 25 August 2026 | re-arming implies the bit had been clear and something armed it, where its resting state is **set**. An erase below `0x020000` does not restore an earlier permission, it is the only thing that ever grants one. `interlock` is the word that survives and `latch` is not, since a latch holds a state something else set and this holds its own |
| `No architecture's external path was followed far enough` | section 186, corrected on 27 August 2026 | said of whether the firmware erases before it programs. The Harmony 600's was followed to the medium by the blind reading of 27 August 2026 and verified here: its program path loads serial flash opcode `0x02` and never `0xD8`, and the eraser at `0x1745C` is a separate routine whose only two references sit on the erase command's path. So programming does not erase there, and the rails' assumption that the caller erases is measured on that architecture rather than assumed on all of them. The Harmony One is still open and now for a stated reason, that its store ends in a resident call gate below the image base |
| `sets of button bindings` | section 176, corrected on 26 August 2026 | base slot 9 holds **lists** on a stack, not sets. Key lookup walks them top down and the first match wins, so the ordering is the mechanism: it is what lets one list override another rather than merely coexist with it, and an unordered set cannot express that. The entries' enter and leave handlers, recorded structurally by section 67 with no reading, are the push and the remove. Reported by trelowney out of the Harmony 525's firmware; what this corpus adds is that the push and remove counts are fixed per architecture whatever a config holds |
| `a set the interpreter keeps` | section 176, corrected on 26 August 2026 | the `actions.ts` wording for the same thing, and the two instructions it described moved from `placement` to `meaning` with it: `0x1F` high byte `0xFE` pushes a base slot 9 binding list onto the key lookup stack and `0xFD` removes one. Section 73 read the routine and stopped, which is what a placement reading is |
| `every arch 10 reader stays gated` | section 184, on Danny's call of 26 August 2026 | the mapping section 183 derived from content is adopted, so a Harmony 890's screens, button bindings, mode pages, action lists, build timestamp and drawn text all read. The gate is not gone, it moved: `archSlot` throws for one of the **five** base slots arch 10 does not have, since returning a number would hand a reader the neighbouring section. `INSERTED_SLOTS` still has no arch 10 entry and never will, because an insertion list cannot say a base slot is absent |
| `three base slots are absent` | section 184 | five are: 0, 2, 8, **13 and 14**. Section 183 placed base slots 4, 6, 13 and 14 by order between placed neighbours and named those four rows as the risk; two of the four were wrong. Base slot 13's refutation is the strongest measurement on arch 10, because section 130 gives it a closure across sections: its first seven records hold the build timestamp's own fields, and no run of pointers in either payload has targets carrying those seven values, at any field offset 0 to 8 and either width, where an arch 8 container hits exactly once. Base slot 14 is refuted on shape, its section count being one byte on every architecture where raw slot 17's is two |
| `six raw slots are not base slots` | section 184 | eight are: 1, 2, 3, 7, 8, 13, **16 and 17**, the last two being where base slots 13 and 14 had been placed by order |
| `four follow by order` | section 184 | no row of the arch 10 mapping is placed by order any more. The two that survived were confirmed on their contents, base slot 4 as a 125 byte event map holding 30 entries exactly as on arch 8 and base slot 6 as a mode table whose every record carries a screen program that decodes, 137 of 137 and 169 of 169. **The lesson is section 183's own sentence turned around**: a slot with only one home under a monotone mapping is not thereby a slot that is there, and the argument that placed four rows equally permitted the two absences that turned out to be true |
| `having a Harmony 890's alphabet is not having its words` | section 184 | it is, once the mapping is on: 4255 of 4255 drawn glyphs read on the Harmony 890 and 5808 of 5812 on the Harmony 895. Section 180's reason was correct and was about the mapping rather than about the text, so what changed was the premise. The reading is held up by whose words they are: 199 of the Harmony 890's 216 distinct strings also appear on the arch 8 Harmony 880 from the same household, including the same four activity names and the same four appliances, decoded through a per config glyph numbering with no reason to line up between two containers |
| `a Harmony 890's devices, activities` | section 183's list of what ungating would buy, corrected in section 184 | those two are the ones it did **not** buy, and the reason is structural rather than pending. Both routes need what arch 10 does not have: section 86 reads a name from base slot 0's tree, which arch 10 has no slot for, and section 126's fallback reads a device's label from base slot 13's transitions, which section 184 refuted. `inventory` reports four devices on the Harmony 890, since a device is an infrared group, names none of them, and finds zero activities. The activity **names** are on the screen, so what is missing is the chain from a key to an activity rather than the words |
| `the Harmony 880 draws 99 strings the 890 does not` | section 185 | they are the Harmony 890's too, and section 184 recorded a gap in our reader as an unexplained difference between two remotes in one household. 49 of the 890's mode page programs open with screen opcode 22, whose operand width arch 10 had no entry for, so the reader abandoned them and their text with them. With the width read the counts reverse: 318 distinct strings on the Harmony 890 against 298 on the Harmony 880, 277 shared. **An absence measured through a reader is a claim about the reader**, and a difference between two configurations is the shape most likely to be mistaken for a fact about the product |
| `4255 of 4255 glyphs on the Harmony 890` | section 185 | 5634 of 5634, the missing fifth being the text of the 49 programs above. The Harmony 895 goes from 5812 to 6490 drawn with 6486 read |
| `group 9 is absent on every architecture but arch 12` | section 185 | arch 10 declares **fourteen** parameter groups where nine was the most anywhere else, so group 9 exists and `lightBandExtras` fired on a Harmony 890, attributing twelve of its bytes to a display light band read out of a Harmony One's firmware at `0x249A0` and `0x2492E`. The gate was on the group existing and is on the architecture now. Found by the accounting's overlap detector, which the reader's own docstring said was what to rely on here |
| `The internal bootloader does not test a key` | section 189, corrected on 27 August 2026 | it does. There is one port read in the Harmony One's bootloader, `MOVFF PORTB,gprF3B` at `0x0095A`, inside the routine the boot decision calls, and the scan itself reaches the keypad over the external memory bus rather than through a port, which is why counting `PORTx` accesses understated it. Section 87 had the right answer in the same document throughout. The test behind the dead claim missed the read twice over: `MOVFF` was absent from its list of reading mnemonics, and a `MOVFF` carries no addressing mode field, so a guard written as `fields.get('a') != 0` discarded it before the port check ran |
| `zero port reads so it does not test the key` | section 189 | the `CLAUDE.md` wording of the same dead claim. A guard that selects one addressing mode silently discards every instruction that has none, which is the transferable half; `pic18_trace.py` does not have this defect and its docstring says so, and the test hand rolled a scanner instead of using it |
| `the mechanism is narrowed rather than closed` | section 189 | closed. Safe mode entry on arch 12 **is** the internal bootloader: it scans the keypad before anything else, and a byte of `0x0E` keeps it resident in a USB service loop at `0x002E8` that never returns. What stays open is which physical key carries that code, which section 48 says firmware cannot answer on arch 12 because sixteen buttons share one sense line |
| `which reaches the configuration words` | section 189, corrected the same day it was written | said of the `0xC4` an unreferenced routine writes into `EECON1` in both bench bootloaders. Bits 6 and 7 of that register are **unimplemented** on the PIC18F87J50 and PIC18F67J50, per gputils' own headers, so the value is `WREN` and nothing more. The reading was the generic PIC18 layout, where those bits are `CFGS` and `EEPGD`, and it would have been correct on the Harmony 525's PIC18F4550. **The SFR bit layout is per part exactly as the register map is**, which is the same pitfall this project has recorded twice one layer up, and it manufactured a claim about configuration memory out of a routine that writes ordinary program flash |
| `set the working address to` | section 189, corrected the same day it was written | said of command `0x00` of the bootloader's own protocol, whose two stores into the endpoint buffer were read as an address being set when they are a **reply** being filled in: the reply length is four and the two bytes are `0x00` and `0x02`, so it is a version or identity query. Endpoint 1 OUT and endpoint 1 IN point at the **same** buffer at `0x0420`, which the buffer descriptors state outright, so the direction of every field was there to be read and was inferred from the arithmetic instead |
| `scans the keypad before anything else` | section 189, correcting section 87 | two things happen first on the Harmony One. The external memory bus is configured, and then one byte at external `0x020024` is read and rejected if it is `0x00` or `0xFF`, which are what blank and zeroed flash return, so the routine asks whether external flash is programmed. The keypad scan is **skipped** when that fails, and the path falls through to the image validator. It is not a hole in the recovery route: with external flash intact the key test runs, and with it blank the bootloader reaches its USB loop without needing a key at all, so a host can reach the remote either way |
| `this programmer is not shown to restore a config` | section 191, correcting section 189 | true of the **bootloader**, whose write paths are all `EECON1` based, and written as a claim about recovery. The safe mode image one level up carries its own external NOR programmer, 601 bytes byte identical to the resident library at `0x01E018` that the application calls, with the AMD command set and separate erase and program entry points. So a Harmony One can write the flash a config lives in. Say which level: the bootloader cannot and safe mode can |
| `is not in anything this project holds` | section 191, correcting section 186 | said of the instruction that puts a byte into the Harmony One's parallel NOR. It is in `0xFF` `+0xE000`, in the lab since section 22, reached through the call gate section 186 correctly identified. The sentence is a **blind reviewer's**, scoped to a packet that deliberately excluded the internal pages, and it was copied out of that scope into a claim about the repository. A withhold list can produce a wrong claim as well as a missing one |
| `safe mode's host side transfer protocol is unread` | section 192, correcting section 191 the same day | true when written and short lived, because there is no second protocol to read: safe mode's dispatcher carries six of the application's seven commands with the same command bytes **and** the same state numbers, absent only `0x70` infrared capture, and both flash commands reach the programmer section 191 identified. What stood between this project and a rehearsed restore was a rehearsal, not a reading |
| `the internal flash arm is dead code` | section 192, caught before it was published | said of the write and erase target selector on the Harmony One, on the strength of a grep that found only `SETF` and `CLRF` on it. The value `0xFE` **is** written to it, by a `MOVWF 0xee,B` under `MOVLB 0x1`, which is the access bank spelling of the same variable and is invisible to a search for the resolved name. Same family as the `EECON1` confusion in section 189 one layer along, and a seventh site that resets the selector was missed the same way. **Enumerate a variable's sites through the disassembler's own bank tracking, never through a text search for one spelling** |
| `the application cannot write internal program flash` | section 192, caught before it was published | produced by a byte pattern search that reported the classifier's internal arm present in safe mode and absent from the application. The pattern contained the variable's address and the application keeps its copy one bank up, so the search compared spellings rather than logic. **A byte pattern containing a register address is a pattern about one image**, which is also how the `0x40` external ceiling appeared to be present on arch 14: that pattern matched unrelated code, and reading the two routines gives `0x20` there |
| `a different transport is not a different storage model` | section 193, corrected the same day | offered as the reason `0xC112` to `0xC115` stays claimable. True, and not the reason. The operative rule is that refusing more is safe against damaging a remote and is **not free**: excluding the file based family costs nothing because there is no transport here to drive one, and excluding that range would make a Harmony 890 unopenable, where arch 10 is an architecture this project reads. Exclude where we provably have no protocol, not where we might have one and cannot verify the reason to refuse. Danny also caught the neighbouring slip, that concordance's `ZWAVE` macro name was quoted as though it described the hardware, where no device here has ever presented an id in that range |
| `nibble BCD: 0x1600 is 1.6` | section 194 | said of the header word at `0x08`, whose two nibbles were printed as a major.minor version. The byte's value **is** the pointer count: `0x14` is 20, `0x15` is 21, `0x16` is 22, `0x17` is 23 and `0x0F` is 15, exact on all 25 containers here across six architectures. The nibble label is plausible for `0x14` to `0x17` and renders `0x0F` as "0.15", which would make a Harmony 350 older than a Harmony 880. `format_version` survives as a label because the documents and golden vectors carry it; `stated_pointer_count` is the reading. **The phrase is narrowed to name the container header**, because the firmware image's version byte at `0x07` genuinely is nibble BCD, `0x34` being 3.4, so the bare phrase is alive in `src/harmony/firmware.py` and a blanket ban would have been wrong |
| `three format versions and three pointer table lengths` | section 194 | offered as two dimensions of the container's validated span, in four documents. It is **one property counted twice**, since the format word is the pointer count, so the coverage claim was one dimension narrower than stated. A span is a claim about independent dimensions, which is exactly why a redundant entry in one inflates it |
| `pointer_count_known` | section 194 | a container check that tested membership of the hand written tuple `(20, 21, 22)`. It stopped at 22, so every arch 10 container reported as unrecognised while its own header had been stating 23 all along, and it bundled two questions: whether a file is internally consistent, and whether this project has a slot layout for that length. Only the first is a property of the file. `format_states_the_pointer_count` is the replacement and can fail on a container whose header disagrees with its own marker; the layout question belongs to `SLOT_MAPS` and to `archSlot` refusing an absent base slot |
| `no path in the vendor's own software returns a remote to a factory state` | section 196, correcting the ledger in `docs/host-client.md` | offered as the honest negative after reading command `0xE0`'s four sub-commands in Logitech's client. Both MyHarmony clients carry a hidden recovery tool whose first button is a factory reset per model, Alt-F9 in the Silverlight one, and it was performed on a Harmony Touch on 27 August 2026. **It is not a command**, it installs a whole factory firmware image fetched over the network, so no amount of reading the command protocol could have found it and finding it says nothing about `0xE0`. Published for the Linux generation only |
| `the only Harmony firmware images anybody has published` | section 196, said of three `.hfw` packages in `reference/checksums.md` | dead twice. Arch 8 gained firmware on 10 August 2026, contributed rather than published, sections 113 and 116; and Logitech's own software update service serves eleven images to an anonymous request, one of which is the **fourth** package of that kind and covers the Harmony 300 and the Harmony 350. The scarcity was real when written and the sentence outlived it by a fortnight |
| `no address, no firmware, no RAM` | section 196, correcting section 193 | said of the file based family and true of the **USB protocol**, which is what section 193 was about. It read as a claim about the project, and the firmware half is now false: the Harmony 300 and 350 image is served by the vendor, is an ordinary PIC18 image at `0x9000`, and reads with no new code. The storage still being addressed by filename and the processor being a PIC18 were never in tension. Say which surface a reach claim is about |
| `none of it ever crossed into this repository` | section 198, correcting section 197 | said of the reading the lab notes recorded on 9 August 2026. The architecture and codename table had not crossed; the **infrared learn session had**, thoroughly, on the same day: `docs/host-client.md` carries all four commands with their byte values, the entry point table whole, the restart command identified as `WRITE_MISC` selector `0x0A`, section 97's firmware confirmation of that table's order, the fact that the selector is a no operation on arch 12 so the client's own first command is ignored by the remote it is sent to, and the rail that an implementation belongs behind `WRITES_ENABLED`. The check that would have caught it is reading the repository before saying what is not in it, and the reason it was skipped is that the sentence felt like the point of the section. **A process failure is not evidence for itself** |
| `fifteen fields` | section 201, correcting section 200 | said of the identity file a Harmony Touch returns. It states **fourteen**, and the fifteenth was our own reader returning the transfer's NUL padding as content, which `parseSysInfo` then made into a field, since `trim()` strips whitespace and NUL is not whitespace. The reader now cuts a read to the size the open reply states, which is the only place a file's end is stated on this protocol. **A count taken off a reader is a claim about the reader**, and this one had no closure against anything: the number of fields was neither predicted nor checked against the firmware's own list, which names four fewer |
| `a final partial packet can be read without knowing the file size first` | section 201, correcting section 198 | the packet's size field is the size of the transfer unit and the last packet pads to it, so nothing in the stream marks where the file ends. Read off the templates, where the field genuinely exists and its meaning had to be inferred, and refuted by two files whose real content stops exactly at their stated size with 41 and 14 bytes of NUL after it |
| `and the richest one` | Danny's instruction of 28 August 2026 | said of Harmony Desktop's web application as a source. **MyHarmony is the reference client**, because everything the newer one can do MyHarmony can do too and better, so a reading off Harmony Desktop describes the hub generation rather than the remotes on this bench. Measured the same day: the compile was driven from its call site and the Harmony Touch's compile failed, where MyHarmony uses a different operation and a differently spelled argument. Its mirror keeps two uses nothing else here covers, the per skin protocol templates and the parameter encoder, both confirmed against hardware |
| `a different operation of the three the service offers` | section 202, correcting itself the same day | said of how MyHarmony compiles a configuration. It sends `StartCompileWithLocaleAndSettings`, the same operation this project already sends; `StartCompileWithLocale` is the on screen simulator's, `CompileEZSim`. The claim came from finding a method name in a compiled assembly and reading adjacency as a call site, with 1999 files of the same client decompiled to C# sitting in the lab unmentioned by the register. **A name is not a call site**, and what separates them is the surrounding code a string search cannot see |
| `our client for their live services` | section 202 | the lab register's description of `work/myharmony/src/`, which is **MyHarmony decompiled**. Ours is `probe.py` one directory up. The row was accurate about the parent and wrong about the square, and it is why a session searched compiled assemblies for a flow that was available as source |
| `Firmware first, always` | Danny's decision of 28 August 2026 | the ordering rule for where to look, now reversed: **read Logitech's own client first, then derive**. It made the cheapest source the last resort, so the expensive work happened first by default and the check that would have prevented it came after. Measured cost: nineteen days of an unread protocol map, section 197; six rounds of hardware guessing a framing that was one function of theirs, section 200; and a wrong reading published in four documents while the source refuting it sat in the lab, section 202. **What did not change is what confirms a fact**: the firmware is still the authority and still wins a disagreement, and their code still never travels into this repository |
| `where the firmware genuinely cannot settle something` | Danny's decision of 28 August 2026 | the condition that used to gate reading Logitech's client at all. There is no condition now: it is read first, every time, and the firmware's authority is a separate question from where a lead comes from. The two had been collapsed into one rule |
| `deliberately still claimable` | section 207 | of `0xC112` to `0xC115` in `isHarmony`. Those product ids are the tunnelled family: Logitech's own client gives them a datagram transport with named services rather than command reports, and concordance's class for them opens by initiating a TCP channel. A Harmony 890 was never openable here and its configs arrived as files. `isTunnelledRemote` reports them and `openHarmony` refuses them |
| `all of that is upstream's label that` | section 207 | of concordance's `ZWAVE` macro over the same range. It was unverifiable while the client had not been read for it; Logitech's own factory chain agrees with concordance on the boundary and on the reason, so the label is corroborated rather than merely upstream's |
| `named by the client and placed on nothing. No reason found` | section 208 | of skins 50 and 58, Khalua and Baileys, in `reference/models.md`'s table of the skins the platform join could not place. They are the Harmony 670 and the Harmony 620, both on architecture 7, so the reason is the same reason as the other nine: the old 6xx and 7xx family is matched by product id and carries no skin list. What was missing was not a statement about platforms, it was that nobody had asked what architecture those models are |
| `the HID layer is the exception and is fully mined` | section 209 | of `software/classic/src/` in the lab register. `hid/commands/` is mined and `hid/services/` had never been opened, and saying the layer made a re-derivation of the first look like new ground in the second. A status of `mined` on a directory is a claim about a whole subtree made from having mined part of it, so the honest granularity is the directory somebody actually read |
| `the allow-list is what makes the compare identify the` | section 188 | of `rehearse-block.ts`'s dump allow list. It proves **content** and not identity, so another Harmony One whose selected block matched the spare's dump would pass as the spare. That no two present lab dumps share a block is a control for today, not a proof, and `targetIsTheSpareRemote` remains a caller assertion the library cannot check |
| `transfer ends in a one byte chunk never terminates` | section 94 | of the internal program memory read. The cause is the count's **parity**: the fetch loop reads a word, subtracts two and exits on zero, so every odd count runs away and 65 and 127 hang exactly as 63 does. The rail refuses odd counts and always did; only the reason was wrong, and the reason is what a session reasons from about other read shapes |
| `refuses more than 62 bytes` | section 96 | of `readInternalMemory`. There is no length cap: 124 bytes is two chunks and reads fine, while 65 and 127 hang. The only refusal is of an odd count |
| `matching the connected remote's protocol, skin, board and flash id` | section 87 | of the `INTENDEDVERSION` compatibility gate, the lower case spelling of the row above. It compares **six** fields, those four plus `SOFTWARETYPE` and `ARCHITECTURE`, and two fields short is a gate that can pass a config built for a different remote |
| `built for: protocol, skin, board and flash id` | section 87 | the same gate, as the glossary defined it. Six fields. The bare four field list is deliberately **not** the phrase here, because a Harmony 525's `GET_VERSION` reply really does carry those four among its seven, so the wording is dead only where it describes the gate |
| `the block size follows from the addressing rather than from a chip table` | section 192 | of `ERASE_BLOCK_SIZE` on arch 12. The forced top byte fixes a 64 KiB **addressing window**, which bounds what one command reaches and says nothing about the chip's erase sector: `docs/host-client.md` itself records the bottom 64 KiB erasing in 8 KiB blocks under that same window. The figure is still the client's word and still unconfirmed |
| `killed a Harmony One three times out of three` | the lab notes of 23 August 2026 | of the oversized sequence. The measurement is that it **hung** three times out of three under heavy tapping, with the batteries taken out each time, so the remote recovered every time. The hazard is real and reproducible and nothing was destroyed |
| `the user config region runs to exactly` | section 88 | of both architectures at once, in `docs/memory-map.md`. Arch 12 reaches `0x400000` and arch 14 stops at `0x200000`: the firmware refuses every flash address at or above it, and the validator's bound, the `FLASH` capacity byte, the part number and the bench remote all agree on a 2 MiB part. concordance's 3904 KiB assumes a 4 MiB part and is the figure this was derived from |
| `3904 KiB is the obvious guess` | section 88 | for the Harmony 700's config region, in `docs/memory-map-700.md`. The guess was carried over from the Harmony 600 figure the same section refuted, so it put a dead number back into circulation. The architecture's own `0x200000` bound is the answer available without a 700 on the bench |
| `six of the maxima are fixed` | section 130 and `sections.ts` | of base slot 13's clock records. **Five** are constants, `59, 59, 23, 6, 11`. Two move with their value and both have to be stamped: the year's is that year plus one, and the day of the month's is 30 in every container here because none was built on a 31st, so a save on a 31st writes a day of 31 into a variable whose stated range stops at 30 |
| `It initialises the remote's clock from it` | section 138 | of base slot 3 on arch 12. Base slot 13's records **are** the clock, seeded from each record's own `first` at `0x2A266`; base slot 3 is the epoch subtracted against at `0x27F20` to work out how long ago the config was built. The two agree at boot because one generator stamps both from the same moment, which is what made it look like a copy |
| `the narrower population a corpus wide **total** is computed from. It leaves the` | section 140 | `CONTAINERS` is 19 and **includes** the Harmony One sync pair. A config before a sync and the same remote's config after one are two containers, because the bytes differ; a total over containers is not a headcount of remotes. What it does leave out is the two arch 8 configs |
| `151 distinct` | section 164 | tail shapes across the corpus. It is 140: merging adjacent durations of one kind took a reading away from 45 records and eleven shapes with them. The second half of that sentence, that no rule lies behind them, is dead too since section 171 gave 31 of the rhythm table's 38 entries a per family `tail` |
| `with no rule behind them` | section 171 | of the infrared tail. A rule exists for 31 of the rhythm table's 38 entries and the encoder emits whole blocks for them; only the remaining seven are copied because nothing predicts them |
| `on every one of the 3547 records` | section 164 | that read as a frame. 3502, for the same merge that moved the tail count |
| `Three families here are of this kind` | section 163 | of the biphase families, in the rhythm table and its generator template. There are four: `Microsoft 30 Bit`, `Kreatel IP 22 Bit`, `Magnavox 13 Bit` and `Philips RC5 13 Bit Toggle` |
| `the biphase ones are not in this table at all` | section 163 | of `stated.ts`. All four are in it, each with a `biphase` shape reproducing its own records exactly, and the encoder in the same file emits them. The dead paragraph named `Microsoft 30 Bit` as what could not be emitted, which is one of the four |
| `4 and 6 cut every record here identically` | section 164 | of the frame boundary factor. They cut 72 of 2777 records differently. The claim the parameter exists to check is that the value sits inside a gap, not that two values agree |
| `nothing here speaks their protocol` | sections 200 and 201 | of the file based family. `packages/usb/src/filepipe.ts` implements it and a Harmony Touch's `/sys/sysinfo` was opened, read and closed on the bench. `openHarmony` still refuses those product ids, because the **config** path does not reach them, which is a narrower claim |
| `a Harmony\nTouch refuses an open without it` | section 200 | of the file protocol's ping. Measured the other way: a bare ping draws no reply and the open after it draws none either. What was missing was the string parameter's encoding |
| `which is why it already sits behind the write flag` | section 188 | of `START_IRCAP`. It is refused by `READ_ONLY_COMMANDS`, not by `WRITES_ENABLED`: no method sends `0x70`, no rail mentions it, and lifting the flag would enable nothing |
| `calls itself `Harmony Remote 4-3.4.0`` | section 189 | of a normally booted Harmony One. `4-` is the safe mode image's string and the application's is `0-3.4.0`, which matters in the docstring of the predicate whose job is telling device states apart |
| `these remotes have no config address` | section 199 | of the file based family. The remote's own file table gives `/cfg/usercfg` external flash `0x020000` for 256 KiB on a Harmony 350. It is the **host** that has no address, because the protocol offers no way to ask for one |
| `only open, read and close are` | section 198 | implemented in the file protocol guard. The allow list holds four commands: ping, open, read and close |
| `The bound is 12 ms, set from that control` | section 139, corrected 29 August 2026 | the bound is a **ratio**, not a time: one pass of hashing every glyph is timed in the test, and the whole resolve must cost under four of them. A fixed millisecond bound could not work, since the healthy case, the broken case and machine noise all sit in one band |
| `the vendor's own jars and tooling` | section 214 | of `software/classic/tools/`. Not one of its 616 files is Logitech's: two Java decompilers and a 361 MB JDK, which is this project's own toolchain |
| `the shipped application and the untouched original` | section 214 | of `software/classic/dist/` and `orig/`. The first is **our repack**, dated 7 August 2026 against the vendor bundle's 28 March 2022, and it adds two classes of ours that read a remote to disk |
| `probably the same code as `software/classic/orig/`` | section 214 | of the standalone macOS bundle against the unpacked original. The copy to compare it with is the repack, and this one is the pristine vendor build |
| `the remote can name its own regions` | section 214 | of `getRegionIds`. The list is computed host side from the version block, gated on a bootloader version, and no packet is sent. The dead wording is ours, written in the lab on 7 August 2026 |
| `no counterpart anywhere in the lab` | section 215, corrected the same hour | of the Harmony One's embedded config read by Logitech's client. Its 8902 byte body is identical to `one_safemode`, cut out of a firmware image. The search that found no counterpart looked only under the dumps directory at small files |
| `the vendor's device icon set` | section 216 | of `reference/logitech-icons/`. It is ours: an extraction we made out of their software, with our own scripts and manifest beside it. The artwork inside it is theirs and still unpublishable |
| `Nothing anywhere recorded that it stopped` | section 214, corrected 30 August 2026 | of the abandoned attempt to run Logitech's client without its server. `software/classic/README.md` records it the same day, with both reasons and what the thing became instead. What is true is that nothing in **this repository** recorded it |
| `and **not** in the schema` | the completeness pass of 30 August 2026 | of the inter key and inter device delay fields on a device. They are in the schema, on `AbstractDevice`, which `Device` extends. The reading looked at a device's own field list and not at what it inherits, and so did the test that checked it, so the two copies agreed and neither was right |
| `for every chip it lists against arch 12 the layout is` | section 221, 30 August 2026 | of Logitech's client's flash block table. It has three arch 12 rows and two different boot layouts, and the one that sentence went on to quote belongs to a chip neither bench Harmony One carries. They report `1F:C8`, whose row is eight 8 KiB blocks and then sixty three of 64 KiB. What survives is uniform 64 KiB above `0x010000`, which is all any rail here uses |
| `probes for the capability rather than requiring it` | section 224, 30 August 2026 | of `HarmonyRemote` against the guarded transport. The probe was a duck-type check for a public `authoriseReport` **method**, which is exactly why the capability to permit any report was in every caller's hand: two lines reached `ERASE_FLASH` at `0x3D0000` with writing disabled. The permission is a `WeakMap` in `authorise.ts` now and there is no capability on the object to probe for |
